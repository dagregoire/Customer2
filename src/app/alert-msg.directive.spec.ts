import { AlertMsgDirective } from './alert-msg.directive';

describe('AlertMsgDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertMsgDirective();
    expect(directive).toBeTruthy();
  });
});
